const path = require("path");

const rc = {
  config: path.resolve(__dirname, "sequelize/database.js"),
  "migrations-path": path.resolve(__dirname, "sequelize/migrations"),
};
console.log("sequelizerc", rc);
module.exports = rc;
